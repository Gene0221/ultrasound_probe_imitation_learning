robot:
  ip: "172.16.0.2"
  pose_source_field: "O_T_EE"
  fallback_pose_source_fields:
    - "O_T_EE_c"
  connect_timeout_s: 10.0

sampling:
  target_hz: 30.0
  max_samples: null

output:
  output_root: "output"
  jsonl_file_name: "franka_ee_pose_deltas.jsonl"
  summary_file_name: "summary.json"
  write_jsonl: true
  emit_stdout_records: false

recording:
  include_absolute_pose: true
  include_pose_source_field: true
  include_robot_ip: true
