# Launch defaults for trajectory replay.
# Command-line options still override these values.

ROBOT_IP="172.16.0.2"
REPLAY_INTERPOLATOR="quintic" #option: quintic & bspline
TRAJECTORY="./config/replay_trajectory.csv"
REPLAY_MODE="relative"

SPEED_SCALE="1.0"
MAX_TRANSLATION_SPEED="0.2"
MAX_TRANSLATION_ACCELERATION="0.07"
MAX_ROTATION_SPEED="0.35"
MAX_ROTATION_ACCELERATION="0.5"
RAMP_TIME="3.0"
HOLD_AT_END="1"

ENABLE_FORCE_CORRECTION="0"

# Used only when REPLAY_INTERPOLATOR="bspline" or --interpolator bspline.
# Default requested after plotting comparison: 0.0016.
BSPLINE_SMOOTHING_FACTOR="0.0016"
